{
  "name": "do-shooter",
  "version": "1.0.0",
  "description": "Game server with Colyseus on Cloudflare Durable Objects",
  "main": "src/server.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "colyseus": "^0.15.0",
    "@colyseus/schema": "^2.0.0",
    "@colyseus/ws-transport": "^0.15.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
