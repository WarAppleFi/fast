{
  "name": "js-multiplayer-3d",
  "version": "0.1.0",
  "scripts": {
    "build": "worker-build --release",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "worker-build": "^2.0.0"
  }
}
